@model IEnumerable<HotelManagement.Models.Amenity>

@*
    For more information on enabling MVC for empty projects, visit https://go.microsoft.com/fwlink/?LinkID=397860
*@
@{
}

<div class="col-sm-8">
    <h4 class="d-inline-block">Amenities</h4>
    <a asp-action="Add" class="btn btn-success btn-sm">Add new</a>

</div>

@if(Model.Count() == 0)
{
    <p>You do not have any amenities defined!</p>
}

<table class="table">
    <thead>
        <tr>
            <th scope="col">#</th>
            <th scope="col">Name</th>
            <th scope="col">Edit</th>
        </tr>
    </thead>
    <tbody>
        @{
            int count = 1;
         }
        @foreach (Amenity amenity in Model)
        {
            <tr>
                <th scope="row">@count</th>
                <td>
                    @amenity.Name
                </td>
                <td>
                    <a asp-action="Edit" asp-route-id="@amenity.Id" class="btn btn-success btn-sm">Edit</a>
                </td>
            </tr>


            count++;
        }
    </tbody>
</table>